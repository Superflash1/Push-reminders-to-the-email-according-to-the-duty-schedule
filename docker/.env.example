# Copy this file to docker/.env if needed, then run docker compose --env-file .env up -d

APP_NAME=Duty Reminder
APP_ENV=prod
APP_HOST=0.0.0.0
APP_PORT=8000
SQLITE_PATH=/app/data/duty_reminder.db
TIMEZONE=Asia/Shanghai

# SMTP settings
MAIL_HOST=
MAIL_PORT=465
MAIL_USER=
MAIL_PASSWORD=
MAIL_FROM=
